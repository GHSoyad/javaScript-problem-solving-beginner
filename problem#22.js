// Make an array of the things you have learned so far. Then use a for loop to show each thing you learned as output.

let thingsLearned = ["variables", "operators", "conditions", "loops", "arrays"]

for (let i = 0; i < thingsLearned.length; i++) {
    let eachThing = thingsLearned[i];
    console.log(eachThing);
}
