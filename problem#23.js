// Make an array with the list of phone models you have used so far. Then use a while loop to show each array elements one by one as output.

let phoneModels = ["Siemens", "Motorola", "Huawei", "Samsung", "Xiaomi"];

for (i = 0; i < phoneModels.length; i++) {
    let phoneModel = phoneModels[i];
    console.log(phoneModel);
}