// Write a function called fullName() which will take first part nad last part of your name as parameters. The function will then add the two parts together and return the full name. Like "hablu" and "kanto" are two input parameters and it will return "hablu kanto".Write a function called fullName() which will take first part nad last part of your name as parameters. The function will then add the two parts together and return the full name. Like "hablu" and "kanto" are two input parameters and it will return "hablu kanto".


function fullName(firstName, lastName) {

    return firstName + " " + lastName;
}

console.log(fullName("Hablu", "Kanto"));
console.log(fullName("Jhon", "Doe"));
console.log(fullName("Samantha", "Hawkins"));