// Write a function that will take hour as the input parameter and will convert it into seconds and will return the result in seconds.


function hourToSeconds(hour) {
    return hour * 3600;
}

console.log(hourToSeconds(1));
console.log(hourToSeconds(2));
console.log(hourToSeconds(5));