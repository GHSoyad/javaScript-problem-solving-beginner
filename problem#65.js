// Write a code to show all the numbers greater than 80 in an array.


const array = [45, 100, 50, 470, 80, 30, 200, 48];

for (number of array) {
    if (number < 80) {
        continue;
    }
    console.log(number);
}